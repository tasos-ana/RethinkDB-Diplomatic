bower install angular && bower install angular-animate && bower install angular-aria && bower install angular-cookies && 
bower install angular material && bower install angular-resource && bower install angular-route && bower install bootstrap && bower install jquery

   install dependencies:
     $ cd app && npm install

   run the app:
     $ DEBUG=app:* npm start
